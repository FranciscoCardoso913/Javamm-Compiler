import ioPlus;

Test extends ioPlus {
    .method public static main(args.array.String).V {
        ret.V ;
    }

    .method public add(a.i32, b.i32).i32 {
    tmp0.i32 :=.i32 a.i32 +.i32 b.i32;
    ret.i32 tmp0.i32;
    }

    .method public doSomething().V {
        a.i32 :=.i32 10.i32;
        tmp1.i32 :=.i32 a.i32 +.i32 20.i32;
        b.i32 :=.i32 tmp1.i32;
        invokestatic(ioPlus, "println", b.i32).V;
        ret.V ;
    }

    .construct Test().V {
    invokespecial(this, "<init>").V;
    }
}